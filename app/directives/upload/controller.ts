///<reference path="../../../typings/tsd.d.ts"/>

export = UploadController;

class UploadController {
    static $inject = [
        'htManager'
    ];

    constructor(
        htManager
    ) {

    }
}