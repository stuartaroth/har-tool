///<reference path="../../../typings/tsd.d.ts"/>

export = ExampleController;

class ExampleController {
    static $inject = [
        'htManager'
    ];

    constructor(
        htManager
    ) {

    }
}